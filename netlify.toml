[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Routing is handled natively by the function's `export const config = { path }`
# (see netlify/functions/api.mjs). No [[redirects]] here — having both fights
# with the function's own routes and can 404 every path.
